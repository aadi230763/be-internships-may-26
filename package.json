{
  "name": "signals-challenge-node",
  "version": "0.1.0",
  "description": "A production-leaning signal ingestion library handling rate limits, idempotency, and DB failures.",
  "main": "src/index.js",
  "exports": "./src/index.js",
  "type": "module",
  "scripts": {
    "dev": "node src/server.js",
    "test": "node --test tests/*.test.js",
    "bench": "npx autocannon -c 20 -d 10 -p 3 http://localhost:8080/healthz",
    "fmt": "prettier --write .",
    "lint": "eslint ."
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "autocannon": "^7.14.0",
    "eslint": "^9.21.0",
    "eslint-config-prettier": "^10.0.1",
    "globals": "^16.0.0",
    "prettier": "^3.5.2"
  }
}
